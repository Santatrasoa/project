#ifndef __VERIFICATION____
#define __VERIFICATION__
int verif();
int test(char * user, char * password);
int verifUser(char * string);

void writeVerif(int flags);
void addUser(char * user, char * password);

#endif
